import { Meta, Title, Subtitle, Description, Primary, Controls, Stories } from "@storybook/blocks";
import * as ProductCardStories from "./components/ProductCard.stories";

<Meta of={ProductCardStories} />

<Title>React Shop UI</Title>

<Subtitle>MUI design system for the microfrontend shop</Subtitle>

<Description>
  Shared theme, buttons, headers, and product cards used by the shell, catalog, and cart remotes.
</Description>

<Primary />
<Controls />
<Stories />
